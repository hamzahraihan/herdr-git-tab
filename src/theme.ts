/** Selected-row wash: a dark slate background that reads as a subtle highlight
 *  in dark mode. Replaces `inverse`, which renders as a harsh full-white block
 *  in most terminals and wipes out dim foregrounds (author, age). All row
 *  foregrounds (yellow/green/white/gray) stay legible on top of it. */
export const SELECTED_BG = "#2e3440";
